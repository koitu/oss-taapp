[project]
name = "telemetry-api"
version = "0.1.0"
description = "Abstract telemetry API for observability"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]