[project]
name = "openai-client-service-api-client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "attrs>=25.3.0",
  "openapi-python-client>=0.27.0",
]

packages = [{ include = "openai_client_service_api_client", from = "src" }]

