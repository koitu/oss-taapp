# Chat Client API

Abstract contract for chat client implementations. This package defines `Client`, `ChatMessage`, and `Channel` abstractions used by Discord and other implementations.

See `src/chat_client_api/README.md` for usage examples and design notes.
