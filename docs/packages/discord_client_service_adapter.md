# Discord Client Service Adapter

Adapter that wraps the generated Discord service client and implements the `chat_client_api.Client` interface, allowing user code to call the same API whether using a local implementation or a remote service.

See `src/discord_client_service_adapter/README.md` for implementation notes and examples.
