# mail-client-service-client

This is the generated OpenAPI client package for the Mail Client Service (copied from `src/clients/README.md`).

Overview:

- Provides typed HTTP client wrappers (Client, AuthenticatedClient)
- Exposes modules under `mail_client_service_client.api` for each path/method
- Use the `*_detailed` variants to access raw response metadata

Usage patterns and examples are available in the package README in `src/clients/README.md`.
